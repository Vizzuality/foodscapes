pre-push:
  parallel: true
  commands:
    eslint:
      root: "client/"
      glob: "*.{js,ts,jsx,tsx}"
      run: yarn lint {push_files}
    types:
      root: "client/"
      glob: "*.{js,ts,jsx,tsx}"
      run: yarn check-types
